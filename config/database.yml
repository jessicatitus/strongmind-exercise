default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV["DATABASE_URL"] %>

development:
  <<: *default
  database: github_ingestion_development

test:
  <<: *default
  database: github_ingestion_test

production:
  <<: *default
  database: github_ingestion_production
